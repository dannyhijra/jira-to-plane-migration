{
  "name": "jira-to-plane-migration",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "migrate": "bun run src/cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "playwright": "^1.48.0",
    "typescript": "^5.5.0"
  }
}
