# Verification: AR — 2026-05-21T09:14Z (links)

Entity: links (added to scope 2026-05-21 after a link inventory disproved the original "exclude" assumption)
Result: 73 created / 16 skipped / 0 failed = 89 distinct (matches Jira inventory exactly)

Plane project AR (2643c390-7309-4ef6-97cb-9873d372e0c3).

## Inventory that drove the decision

Scanned 1524 AR issues → 89 distinct issue-links:
- **74 within-AR** (migratable) · 15 cross-project (unmigratable — Plane relations are project-scoped)
- By type: Cloners 62, Relates 17, Blocks 8, Duplicate 1, migration_parent 1

The original `/migrate-configure` decision excluded links on the assumption that access-request tickets rarely link within-project. The inventory disproved it (74 within-AR, dominated by `Cloners` from repeat requests), so `links` was added to `migrate_entities`.

## Result

- **73 within-AR relations created** (74 within-AR minus 1 `migration_parent` dropped). Mapping: Cloners/Relates → `relates_to`, Blocks → `blocking`/`blocked_by`, Duplicate → `duplicate`.
- **15 cross-project skipped** (targets in SREA/SREH/INFRA/ACQ/REAL — not in AR's Plane project) → recorded in `state/DEFERRED.md`.
- **1 `migration_parent` dropped** (Jira-internal artifact link, intentional).
- 0 failures.

## Spot check

AR-42 → relations endpoint (`/api/.../issues/{id}/issue-relation/`) returns `relates_to: [AR-43 "[P2P] Request access dev server"]` ✓. Plane stores relations bidirectionally.

## Verdict

Links migration complete and verified. **All AR data entities are now done** (issues, comments, epics/modules, attachments, links).
