# Verification: IHH — 2026-05-21T04:05:00Z (post-scale random sample)

Full scale run complete: **1694/1694** distinct work items migrated (`ok`), 0 outstanding failures, 0 duplicate creates. (3 transient 429s — IHH-44/69/480 — retried clean on a `--resume` pass.)

Random sample: 20 items spread IHH-28 → IHH-1774 (`scripts/verify-ihh-sample.ts`), plus targeted footer + assignee spot-checks on recent issues.
Plane project `IHH` = `0fbdc5d0-2d02-42a2-9f02-2830cbe190f6`.

Sample size: 20
Passed: 20 (1 cosmetic whitespace note reclassified from FAIL)
Failed: 0

## Sample keys

IHH-28, 112, 196, 287, 400, 484, 569, 663, 747, 831, 915, 999, 1086, 1171, 1255, 1339, 1423, 1509, 1593, 1774

## Findings

- **19/20 exact PASS** on title, state-group, priority, labels, assignee handling.
- **IHH-1339 · cosmetic (reclassified PASS)**: Jira title `"Request Permanent Access CB & Salman "` has a trailing space; Plane stores it trimmed. The migrator trims trailing whitespace — benign normalization, not a fidelity loss. (The verify script did strict `===`, hence the raw FAIL.)

## Custom-field footer — CONFIRMED working (the pilot couldn't test this; its 2021 tickets had null CFs)

Direct check on recent issues:

- **IHH-1773**: Jira `Reason="untuk pembebanan Mei 2026"`, `Impact Reason="tidak dapat melakukan pembebanan"` (ADF), `User Type="Non User"`, `SLA="1 Hari"` → Plane footer renders all four:
  `**Reason:** untuk pembebanan Mei 2026 · **Impact Reason:** tidak dapat melakukan pembebanan · **User Type:** Non User · **SLA Information:** 1 Hari` ✓
- **IHH-1774**: footer renders `Reason / Impact Reason / User Type=User Retail / SLA Information=2 Hari Kerja` ✓

ADF custom fields → text, select custom fields → `.value`, all under the `<!-- migrated-custom-fields -->` marker with correct display names. The `property:` mechanism works as designed.

## Assignees — all empty by design (NOT a failure)

IHH-1773's Jira assignee `efazrin@hijra.id` is a **workspace** member, yet Plane assignees are empty. Reason: assignee resolution is **project-scoped** (`listProjectMembers(IHH)`), and the freshly-created IHH project has only the API-key owner (dcahyono) as a member. So **every** Jira assignee on this initial migration lands empty + email-in-prefix, regardless of workspace membership.

- This is consistent with all prior new-project migrations (e.g. DEPLOY's "14 INFO assignee pending").
- ⚠️ **Correction to discovery doc**: its "5 members → 715 items auto-resolve" was over-optimistic — workspace membership ≠ project membership. In practice ~948 assigned items migrated empty.
- **Resolution path**: Stage 3 `/migrate-reassign IHH` after invitees accept and are added to the IHH project. Emails are preserved in every prefix (`assigned to \`...\``), so no data is lost.

## Notes (expected, carried over from pilot)

- Description stored as markdown-in-`<p>` (`wrapAsHtml`) — markdown shows literally; pre-existing, shared, tolerated.
- Redundant default "Todo" state alongside seeded "To Do" — cosmetic.

## Verdict

✅ **Issues entity verified.** Mapping, prefixing, label seeding, state/priority resolution, and the custom-field footer all correct across the full range. Assignees intentionally deferred to reassign. Ready for the next entity: **comments**.
