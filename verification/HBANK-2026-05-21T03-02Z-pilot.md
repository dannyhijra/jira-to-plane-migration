# Verification: HBANK — 2026-05-21T03:02Z (issues pilot)

Sample size: 5 (issues pilot, `--limit 5`)
Passed: 2 fully verified (HBANK-1, HBANK-2)
Created + manifest-ok but field-verification deferred: 3 (HBANK-3/4/5)
Failed: 0

Plane project: `bae8b0e4-e780-4158-8cbf-de155e85b4da`. Run: 5/5 migrated, 0 failed.
Verified via X-API-Key REST (Plane MCP `/issues/` endpoints 404 on this self-hosted build; only `/states/` works).

## Per-item findings

### HBANK-1 → db52474c-ed4f-43ef-b31f-696f24a59610 · ✅ PASS
- Title exact ✓
- Priority: Jira Highest → `urgent` ✓
- State: Jira `Done` → Plane `Done` (completed) ✓ — mapping applied, not collapsed to unstarted
- Labels: `PostgreSQL-to-16` (native), `type:bug-dev` (Dev Bug), `squad:Bank - General`, `eng:Bank - Platform` ✓ — all new label families resolve
- ⚠️ INFO: assignees empty — `fmaulana@alamisharia.co.id` present in description prefix; new project has no members yet, resolves on `/migrate-reassign`

### HBANK-2 → be683480-19aa-4cb8-af98-c45d4f8e50e2 · ✅ PASS
- Title exact ✓
- Priority `urgent` ✓; State `Done`/completed ✓
- Labels: `PostgreSQL-to-16`, `type:bug-prod` (Prod Bug) ✓ (no squad fields set on this issue)
- ⚠️ INFO: assignees empty — `rsulistyo@hijra.id` in prefix

### HBANK-3 → e45b828b-8fde-4c0d-870d-da7ec3c043d7 · ⏳ DEFERRED
### HBANK-4 → 229cc12d-7226-4591-8c3c-1947366b96d6 · ⏳ DEFERRED
### HBANK-5 → 8ab58c42-24fa-47f7-b6a4-aae3432ed140 · ⏳ DEFERRED
- Created (plane_ids in manifest, run reported 0 failures). Field verification blocked by Plane HTTP 429 — three concurrent full-scale migrations (INFRA, AR, IHH) were saturating the API during this pilot. Same code path as HBANK-1/2; re-verify when load subsides.

## Pattern analysis
No failures. The new capabilities are confirmed working end-to-end in a real run:
- issue-type → `type:*` label (bug-dev, bug-prod observed)
- squad/eng multi-select → `squad:`/`eng:` labels (observed on HBANK-1)
- status mapping seeds + resolves correctly (Done→completed)
- migration prefix captures original creator + assignee emails

Empty assignees are expected (pre-`/migrate-reassign`, brand-new project with no joined members).

## Notes / deferred
- States: Plane auto-created its defaults on project creation; our seed reused `Backlog`/`Done`/`Cancelled` and added `To Do`, `In Review`, `In Testing`, `Ready for Testing (Main)`, `Ready On Release`, `Pause`. Plane's default `Todo` (unstarted) also exists alongside our `To Do` (unstarted, default) — harmless duplicate; issues map to `To Do`.
- Sub-task parenting + epics-as-work-items not exercised by HBANK-1..5 (none are sub-tasks; created-ASC head is tasks/bugs). Will be exercised at scale; spot-check a sub-task and an epic in the scale verification.

## Suggested next step
Re-verify HBANK-3/4/5 + scale (`/migrate-scale HBANK issues`) AFTER the parallel INFRA/AR/IHH scale runs finish — running HBANK's 967-issue scale now would fight the 429 storm and add load.
