# Deferred migration work

Outstanding entities deliberately skipped during a project's migration. Each row is a piece of work that must be resumed before the project can be considered fully migrated.

Add an entry whenever `migrate_entities` in `config/projects.yaml` is set narrower than what the project actually contains. Remove the entry once the deferred entity has been migrated and verified.

| Project | Entity      | Scope                                | Deferred on  | Reason                                         | How to resume |
| ------- | ----------- | ------------------------------------ | ------------ | ---------------------------------------------- | ------------- |
| HDR     | attachments | 37 of 54 issues carry attachments    | 2026-05-18   | Explicit user instruction during /migrate-pilot | (1) Add `attachments` to `config/projects.yaml` HDR `migrate_entities`. (2) `bun run migrate run --jira-project HDR --only attachments --resume`. (3) Re-run `scripts/verify-hdr-sample.ts` — INFO findings on attachment-bearing items should flip to PASS. |
| DEPLOY  | attachments | 7 of 51 attachments (`.sql` on DEPLOY-159/161/162/194) | 2026-05-20 | Plane storage rejects `binary/octet-stream` `.sql` ("Invalid file type" 400). **Permanent — not retryable.** Stored as placeholders (filename + original Jira URL preserved); files remain in Jira. | Not resumable as-is. Only path: convert to a Plane-accepted type (e.g. wrap as `.txt`) — likely not worth it; SQL lives in Jira + GitHub release links already. |
| DEPLOY  | links       | 102 of 217 links (cross-project: AC/ECO/HTR/INFRA/II/HLS/Polaris targets) | 2026-05-20 | Plane relations are project-scoped — both items must exist in the same Plane project. Targets are issues from Jira projects not migrated into DEPLOY's Plane project. Recorded as `skipped` in the manifest with the target key. | Conditional: only if those projects are migrated into the same Plane workspace AND Plane supports cross-project relations (unverified). Otherwise accept as permanent. The 115 within-DEPLOY relations migrated fine. |
| INFRA   | attachments | 38 of 332 attachments (mostly 34 `text/html`, plus a few octet-stream/sql/etc.) | 2026-05-21 | Plane storage rejects these MIME types ("Invalid file type" 400). **Permanent — not retryable.** Stored as placeholders (filename + original Jira URL preserved in description); files remain in Jira. The other 294 (png/jpeg/webp/pdf/json/zip/…) uploaded as real assets. | Not resumable as-is. Only path: convert to a Plane-accepted type — likely not worth it; originals remain in Jira. |
| INFRA   | links       | 179 of 288 links (cross-project: SREA/HTR/IT/ACQR/DEPLOY/SREH/ECO/II/AR/… targets) | 2026-05-21 | Plane relations are project-scoped — both items must be in the same Plane project. Targets live in other Jira/Plane projects (incl. already-migrated DEPLOY/II/AR, but in separate Plane projects). Recorded as `skipped`. | Conditional: only if Plane gains cross-project relation support (unverified). Otherwise permanent. The 109 within-INFRA relations migrated fine. |
| AR      | attachments | 4 of 144 attachments (`binary/octet-stream`: 3 `.sql` on AR-840/AR-1373×2, + 1 `.pdf` on AR-1378 mislabeled as octet-stream) | 2026-05-21 | Plane storage rejects `binary/octet-stream` ("Invalid file type" 400). **Permanent — not retryable as sent.** Stored as placeholders (filename + original Jira URL preserved); files remain in Jira. The other 140 (png) uploaded as real assets. | Not resumable as-is. The 1 `.pdf` could be salvaged by forcing `application/pdf` on upload (Jira mislabeled its MIME); the 3 `.sql` would need wrapping as `.txt`. Low value — originals remain in Jira. |
| HBANK   | attachments | 1 of 820 attachments (HBANK-462 `Screen Recording….mov`, **97.87 MB**) | 2026-05-21 | After raising the nginx body-size limit, the 58 MB + 74 MB files uploaded; the 98 MB file still fails with Cloudflare **`524`** (origin timeout — upload exceeds CF's ~100s `proxy_read_timeout`), independent of client bandwidth (a faster connection landed the 74 MB but not this). Stored as 1 placeholder (filename + size + Jira URL); file remains in Jira. 819 of 820 uploaded as real assets. | Raise Cloudflare/origin `proxy_read_timeout` for the upload path, then remove the HBANK-462 `attachment#115968` manifest line + `bun run migrate run --jira-project HBANK --only attachments --resume`. Else permanent; original remains in Jira. |
| HBANK   | attachments (sync) | 1 sync attachment (HBANK-1041 `1041.webm`, **115.4 MB / 120,963,332 B**) | 2026-06-12 | Surfaced during the 2026-06-12 incremental sync. Same class as HBANK-462: the 120 MB video upload is cut off mid-stream (`curl (18) partial file`) because it exceeds Cloudflare's ~100s `proxy_read_timeout`. Not a cookie/auth failure (the other 134 HBANK attachments in the same run uploaded fine). Stored as 1 placeholder comment (`bd2fee4b…`, filename + size + Jira URL); manifest entry marked `ok`/placeholder so sync won't re-flag it. File remains in Jira. | Same as HBANK-462: raise Cloudflare/origin `proxy_read_timeout` for the upload path, then remove the `HBANK-1041#attachment#130444` placeholder manifest line + `bun run migrate run --jira-project HBANK --only attachments --resume`. Else permanent; original remains in Jira. |
| HBANK   | links       | 18 cross-project Relates skipped of 125 (other ends in non-HBANK Plane projects) | 2026-05-21 | Plane relations are project-scoped — both ends must be in the same Plane project. (Separately, 5 links were intentionally **dropped** — 4 Polaris + 1 migration_parent per discovery — not deferred.) 102 within-HBANK relations created. | Conditional: only if Plane gains cross-project relation support (unverified). Otherwise permanent. |
| AR      | links       | 15 cross-project skipped of 89 (targets: SREA×6, SREH×3, INFRA×3, ACQ×2, REAL×1) | 2026-05-21 | Plane relations are project-scoped — both ends must be in the same Plane project; these targets live in other Jira/Plane projects. (Separately, 1 `migration_parent` link **dropped** — not deferred.) 73 within-AR relations created (Cloners/Relates→relates_to, Blocks→blocking, Duplicate→duplicate). | Conditional: only if Plane gains cross-project relation support (unverified). Otherwise permanent. |
