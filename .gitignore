node_modules/
.git-broken/
.env
.env.local
state/manifest.jsonl
state/manifest.jsonl.bak-*
state/failures.jsonl
state/sync-state.json
state/.sync.lock
# Confluence PDF export: manifest, raw debug dumps, and the downloaded PDFs
# (large binaries, staged for the later Google Drive upload step).
state/confluence-manifest.jsonl
state/confluence-debug/
confluence-pdf/
*.log
.DS_Store

# Per-project worktrees created by scripts/local-scheduler/run-migration-isolated.sh
.worktrees/

# Filled-in config files contain sensitive mappings — only the .example files are committed
config/projects.yaml
config/users.yaml
config/mappings.yaml

# Playwright MCP session artifacts
.playwright-mcp/
